# URL Сайта Г (PHP) — страница квеста (переход по «Пройти квест»).
# Пример: после запуска siteG через `php -S localhost:8080 index.php` в папке siteG
# VITE_SITEG_URL=http://localhost:8080

# База API Node-бэкенда siteBack.
# Пример: после `cd siteBack && npm run dev`:
# VITE_API_BASE=http://localhost:4000
